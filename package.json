{
  "name": "route-toute",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "probe": "turbo run build --dry-run=json > /tmp/dry.json; cat /tmp/dry.json"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  },
  "packageManager": "pnpm@10.33.0"
}
